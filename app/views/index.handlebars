<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>AngularJS Vide Chat</title>
    <link rel="stylesheet" href="http://yui.yahooapis.com/pure/0.6.0/pure-min.css">
    <link rel="stylesheet" type="text/css" href="/css/main.min.css">
</head>
<body ng-app="app" ng-controller="MainCtrl">
    <header class="header">
        <div class="home-menu pure-menu">
            <a href="{{baseUrl}}">AngularJS Video Chat</a>
        </div>
    </header>
    <main class="content">

    <form class="pure-form">
        <h1 class="mtl">Create a Video Chat room</h1>
        <fieldset>
            <input type="text" value="{{baseUrl}}/{{id}}" class="pure-input-1-2">
            <a href="{{baseUrl}}/{{id}}" class="pure-button pure-button-primary">Start Chat</a>
        </fieldset>
    </form>
    </main>
</body>
</html>